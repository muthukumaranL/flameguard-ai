# Environments / caches
.venv/
.pipcache/
.tmp/
__pycache__/
*.pyc
.pytest_cache/
.ipynb_checkpoints/

# Data (raw + processed are rebuilt by scripts/validate_dataset.py)
data/raw/
data/processed/

# Pretrained downloads (fetched automatically by Ultralytics)
/*.pt

# Large training artefacts kept out of git (summaries/logs stay)
outputs/training/*/weights/last.pt
outputs/training/_resolved_data.yaml

# Packaged submission (216MB ZIP + unpacked copy) - regenerate with scripts/package_submission.py
# Exceeds GitHub's 100MB per-file limit; rebuilt on demand.
submission/

# Stray loose images accidentally saved to the repo root
/images.jpg
/images (1).jpg

# OS noise
Thumbs.db
.DS_Store
